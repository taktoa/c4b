# This will only work if
# basic support for transfer
# of potential of locals
# across function calls is
# available

func f()
local (x)
  if y>0
    ( y-=1
    ; x=10
    ; f()
    ; while x>0 x-=1
    )

; f()
